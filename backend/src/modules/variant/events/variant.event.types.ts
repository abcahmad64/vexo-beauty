export enum VariantEventType {
  CREATED = 'variant.created',
  UPDATED = 'variant.updated',
  ACTIVATED = 'variant.activated',
  DEACTIVATED = 'variant.deactivated',
  DELETED = 'variant.deleted',
  RESTORED = 'variant.restored',
  PRICE_CHANGED = 'variant.price_changed',
  IMAGE_UPDATED = 'variant.image_updated',
}
