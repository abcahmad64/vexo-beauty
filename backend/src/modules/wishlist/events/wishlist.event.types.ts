export enum WishlistEventType {
  CREATED = 'wishlist.created',
  ITEM_ADDED = 'wishlist.item_added',
  ITEM_REMOVED = 'wishlist.item_removed',
  CLEARED = 'wishlist.cleared',
  MERGED = 'wishlist.merged',
}
